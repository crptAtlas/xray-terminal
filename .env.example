# mode B: Bitquery API token (plans from $49/mo). Without it, mode A (public RPC) runs.
BITQUERY_TOKEN=

# override the RPC endpoints, comma-separated; "#nologs" marks an endpoint without eth_getLogs
RPC_URL=

# fixed ETH/USD rate; skips the public spot API (also the fully-offline path)
ETH_USD=
